#tuples use () no []
rectangle=(200,50);#Si se supone que se tiene un rectangulo se usa una tuple para asegurar que sus dimensiones no se puedan modificar
print(rectangle[0]);
print(rectangle[1]);

#loops with tuples
#Basically it's the same that in lits
for dimension in rectangle:
    print(dimension);

#Writing over a tuple
#Para re-escribir una tupla lo 
